function fun(things, friends) {
  return things;
}

function paintHappyTrees(tree) {
  return tree;
}

// convert to lambda

export const newfun = (things, friends) => {
  return things;
};

fun();
newfun();
